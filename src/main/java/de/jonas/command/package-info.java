/**
 * In diesem Package befinden sich alle Befehle, die mithilfe des {@link de.jonas.handler.command.CommandHandler}
 * registriert werden, da mehrere Befehle in einer Klasse implementiert werden und nur durch die {@link
 * de.jonas.object.command.WaschbarCommand Annotation} eingeleitet werden.
 */
package de.jonas.command;
